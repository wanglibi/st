<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"   
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.dao.StockForecastDao">
	<select id="findForecasts" parameterType="cn.model.Common" resultMap="cn.model.StockForecast">
		select 
			st_id as stId,
			st_name as stName,
			fc_date as fcDate,
			st_fluc_rate as stFlucRate,
			week_fluc_rate as weekFlucRate,
			week_fluc_fcst as weekFlucFCST,
			week_up_rate as weekUpRate,
			week_up_fcst as weekUpFCST,
			lately_times as latelyTimes,
			lately_times_rate as latelyTimesRate,
			lately_times_fcst as latelyTimesFCST,
			fcst as fcst
		from stock_forecast order by fc_date
		limit (#{pageNum}-1)*#{pageSize},#{pageSize}
	</select>
</mapper>